Implement the NSVL redesign for Nashville.com in this existing repository using `design/nsvl-brand-handoff/` (or locate the extracted `nsvl-brand-handoff` folder if placed elsewhere).

First read START-HERE.md and PAGE-MAP.md. Inspect the existing repository, framework, routes, content sources, commerce, booking, authentication and analytics. Follow existing repository instructions and preserve working integrations. Give a short implementation plan, then proceed with the authorized local work. This design brief does not itself authorize deployment, domain/DNS changes or replacement of the existing backend.

Read HOMEPAGE.md, GROUP-TRIP-PLANNER.md, MOBILE-FIRST.md, PAGE-LAYOUTS.md, BRAND-GUIDE.md, CONTENT-AND-DATA.md, ACCEPTANCE-CHECKLIST.md, design-tokens.css and page-designs/README.md. Open all nine PNG design boards listed in PAGE-MAP.md directly with your image-viewing tools. Do not build from the prose alone, and do not reproduce the generic gray wireframe appearance. Follow the source-of-truth order in START-HERE.md.

Scope: homepage, Restaurants, Tours, Neighborhoods, Things to do, Events, Hotels, Shop and Plan your trip; associated relevant detail pages; shared header/footer/search/filter/save components. Preserve existing routes and content. Treat suggested slugs as intentions until inspected; prepare redirects if a change is necessary. Keep any domain migration separate.

Identity: use the authentic approved angular NSVL logo. Use Paper White #F5F3ED and Charcoal Ink #1F2421 as the brand palette, with neutral supporting tones only. No third accent. Keep “Get into Nashville.” as the homepage H1 and “One good plan changes everything.” as the group-planner headline on desktop and mobile. Use the actual logo vector from the repository if available. The pack's reconstructed PNGs are provisional; do not ship paper-white review PNGs or invent a replacement monogram. Record vector/favicon dependencies while continuing other work.

Build mobile first (60% expected traffic): useful search or task entry near the top, compact header, Explore / Events / Shop / Plan bottom navigation. Use the homepage ordering in HOMEPAGE.md. On detail pages, contextual ticket/add-to-bag controls replace rather than stack over bottom navigation. Respect safe areas, keyboards, large text and focus order. Then build the full desktop compositions shown in the boards. Use real semantic components and text, not screenshot-as-page images.

The trip planner is a GROUP-OPTIMIZED planner. Start with occasion (including bachelorette, corporate retreat, family and other groups), headcount, age ranges, dates, budget/scope, stay location and preferences. Reveal group-specific questions. Generate an itinerary using actual records and validate capacity, hours, age/accessibility requirements, transit buffers, budget and available times. Rank feasible options by group preferences and proximity. Expose uncertainty; never invent capacity, prices or reservations. Let users lock stops, swap alternatives and rebuild a day without losing fixed commitments. GROUP-TRIP-PLANNER.md contains the complete contract and acceptance scenarios.

Use existing real data/providers. The owner approves using all AI pictures shown in the boards. Read AI-IMAGERY.md and preserve that imagery direction; do not substitute stock by default. Standalone photo assets still need extraction or faithful reference-based reconstruction and responsive export. The boards are flattened layouts, not individual photo files. Venue facts, dates, prices and example inventory remain illustrative and must use real data. Use development-only fixtures when necessary and document missing integrations. No fabricated availability or success states. Saving is not booking. Outbound clicks are not completed transactions. Do not expose secrets in client code.

Implementation sequence: shared tokens/shell and mobile homepage; discovery/category pages and their details; hotel/tour/restaurant handoffs; commerce variant/cart/test checkout; group onboarding, constraint-aware planner and persistence; responsive refinement and verification. Reuse the existing stack and installed components where appropriate. Maintain SEO routes, metadata/canonicals and truthful structured data. Avoid duplicate full entity pages across categories.

Verify at 320, 360, 390, 430, 768 and 1440px; capture all nine pages at mobile and desktop sizes. Run existing build/lint/type checks and meaningful flow tests. Verify search/filter URL persistence and Back, provider handoffs, product variants/cart/test checkout, group-specific planner scenarios, save/reload, sharing only when backed by durable storage, signup errors/success, keyboard/focus and mobile overlays. Do not place real purchases or reservations during testing.

Deliver working repository changes, responsive screenshots, checks run and a concise list of outstanding asset/data/integration dependencies. Distinguish implemented, tested and blocked work. Do not claim that the optimizer, payments or booking integrations work merely because the pages look correct.

Read AI-IMAGERY.md before handling visuals. It supersedes prior blanket instructions to replace generated photography. Inventory and produce the standalone assets before claiming the image implementation complete.
